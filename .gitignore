# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Build output
client/dist/
*.local

# Data & env (anchor to repo root — bare `data/` also ignored `client/src/data/`
# and kept lineup source out of git, breaking Docker builds on fresh clones)
/data/
*.db
.env
.env.*
!.env.example

# OS & editor
.DS_Store
Thumbs.db
*.log
.cache
.vite
